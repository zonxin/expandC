# 堆(heap)

    // 小顶堆
    MinPQ newMinPQ(int n);
    MinPQ_delMax(MinPQ pq);
    MinPQ_insert(MinPQ pq,Ord v);
    // 大顶堆
    MaxPQ newMaxPQ(int n);
    MaxPQ_delMax(MaxPQ pq);
    MaxPQ_insert(MaxPQ pq,Ord v);
